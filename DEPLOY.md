# วิธีรัน Ward เมื่อใช้ iPad

iPad รัน Node.js เองไม่ได้ (ไม่มี terminal จริง) จึงต้องให้เซิร์ฟเวอร์รันที่อื่น แล้วเปิด Safari เข้าไปใช้
เลือกวิธีใดวิธีหนึ่งด้านล่าง เรียงจากง่ายที่สุด

---

## วิธีที่ 1 — มีคอมพิวเตอร์อยู่ใน Wi-Fi เดียวกัน (ง่ายและเร็วที่สุด)

เหมาะกับใช้งานในหอผู้ป่วยจริง เพราะข้อมูลไม่ออกจากเครือข่ายโรงพยาบาลเลย

บนเครื่องคอม (Mac/Windows/Linux ที่มี Node 22.5+):

```bash
git clone https://github.com/Kingcondom/Ward.git
cd Ward
WARD_PASSCODE='รหัสที่ตั้งเอง' npm start
```

หาไอพีของเครื่องคอม

- **macOS**: `ipconfig getifaddr en0`
- **Windows**: `ipconfig` แล้วดู IPv4 Address
- **Linux**: `hostname -I`

จากนั้นบน iPad เปิด Safari ไปที่ `http://<ไอพีของคอม>:3000` เช่น `http://192.168.1.42:3000`
ทุกเครื่องในวอร์ด (iPad, มือถือ, คอม) เปิดลิงก์เดียวกันได้พร้อมกัน ข้อมูลซิงก์ real-time

> ถ้าเปิดไม่ได้ ให้อนุญาต Node ผ่าน firewall (Windows Defender / macOS Firewall) และตรวจว่าอยู่ Wi-Fi วงเดียวกัน ไม่ใช่ Guest network

---

## วิธีที่ 2 — GitHub Codespaces (ใช้ iPad อย่างเดียวได้ ไม่ต้องมีคอม)

ฟรี 60 ชั่วโมง/เดือน เหมาะกับลองใช้หรือเดโม

1. บน Safari เปิด https://github.com/Kingcondom/Ward → ปุ่ม **Code** → แท็บ **Codespaces** → **Create codespace**
2. รอสักครู่จะได้ VS Code ในเบราว์เซอร์ พิมพ์ใน terminal ข้างล่าง:
   ```bash
   WARD_PASSCODE='รหัสที่ตั้งเอง' npm start
   ```
3. จะมีป๊อปอัป **Open in Browser** ขึ้นมา (หรือดูที่แท็บ **Ports**) กดเพื่อเปิดหน้าเว็บ
4. ถ้าจะให้เพื่อนร่วมทีมเข้าด้วย: ที่แท็บ Ports คลิกขวาที่พอร์ต 3000 → Port Visibility → **Public**

> Codespace จะหยุดเองเมื่อไม่ใช้งาน และข้อมูลจะอยู่จนกว่าจะลบ codespace ทิ้ง — เหมาะกับทดลอง ไม่เหมาะเก็บข้อมูลจริงระยะยาว

---

## วิธีที่ 3 — Deploy ขึ้นคลาวด์ ให้เปิดได้ทุกที่ตลอดเวลา

มีไฟล์ตั้งค่าให้แล้วทั้ง `Dockerfile`, `fly.toml` และ `render.yaml`

### Fly.io (แนะนำ — มีเซิร์ฟเวอร์สิงคโปร์ ใกล้ไทย เก็บข้อมูลถาวรได้)

ทำครั้งแรกจากเครื่องคอม (หรือจาก Codespaces ในวิธีที่ 2 ก็ได้):

```bash
fly launch --no-deploy            # ตั้งชื่อแอป เลือก region sin
fly volumes create ward_data --size 1
fly secrets set WARD_PASSCODE='รหัสที่ตั้งเอง'
fly deploy
```

ได้ลิงก์ `https://<ชื่อแอป>.fly.dev` เปิดจาก iPad ได้เลย มี HTTPS ให้อัตโนมัติ

### Render.com (กดจากเบราว์เซอร์บน iPad ล้วน ๆ ได้)

1. เปิด https://render.com → **New +** → **Web Service** → เชื่อม GitHub repo นี้
2. Render จะอ่าน `render.yaml` เอง (runtime = Docker)
3. ไปที่ **Environment** ตั้งค่า `WARD_PASSCODE` เป็นรหัสของคุณ → Deploy

> ⚠️ ถ้าใช้ Render แพลนฟรี จะไม่มี disk ถาวร — ข้อมูลผู้ป่วยจะหายทุกครั้งที่ redeploy
> ถ้าจะเก็บข้อมูลจริงต้องใช้แพลนที่มี disk (ตามที่ตั้งไว้ใน `render.yaml`)

---

## ติดตั้งลงหน้าจอหลัก iPad (ใช้เหมือนแอป)

เปิดเว็บใน **Safari** → ปุ่มแชร์ → **Add to Home Screen**
จะได้ไอคอนแอป เปิดแบบเต็มจอไม่มีแถบเบราว์เซอร์ เหมาะกับใช้ตอน round

---

## ⚠️ ข้อควรระวังเรื่องข้อมูลผู้ป่วย

การ deploy ขึ้นคลาวด์สาธารณะ = ข้อมูลออกนอกโรงพยาบาล ซึ่งมีผลทางกฎหมาย (PDPA) และนโยบายเวชระเบียน

- ถ้าจะเก็บ **ข้อมูลผู้ป่วยจริง** แนะนำ **วิธีที่ 1** (รันในเครือข่ายโรงพยาบาล) เท่านั้น
- ถ้าจะลองใช้บนคลาวด์ ให้ใช้ **ข้อมูลสมมติ** ก่อน
- ไม่ว่ากรณีใด ให้กรอกแค่ชื่อย่อ/HN ตามที่ระบบออกแบบไว้ อย่าใส่ชื่อ-สกุลเต็ม
- ตั้ง `WARD_PASSCODE` ให้เดายาก และเปลี่ยนเมื่อมีคนย้ายออกจากทีม
- อ่านหัวข้อความปลอดภัยท้าย [README.md](README.md) ก่อนใช้งานจริง
