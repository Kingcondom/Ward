# 🚪 วิธีเข้าเว็บ Ward (ฉบับกดตามได้เลย)

หน้านี้คือทางเข้าที่สั้นที่สุด ถ้าอยากรู้ทางเลือกอื่นทั้งหมด (รันบนคอมในวอร์ด / Fly.io / Render)
ดูที่ [DEPLOY.md](DEPLOY.md)

---

## ก. ใช้ iPad หรือมือถืออย่างเดียว — GitHub Codespaces

### ครั้งแรก (ทำครั้งเดียว ~5 นาที)

**1. ตั้งรหัสผ่านของหอผู้ป่วยก่อน**

เปิด https://github.com/settings/codespaces → หัวข้อ **Codespaces secrets** → **New secret**

| ช่อง | ใส่ว่า |
|---|---|
| Name | `WARD_PASSCODE` |
| Value | รหัสที่ทีมจะใช้ login (ตั้งเองให้เดายาก) |
| Repository access | เลือก **Ward** |

> ข้ามขั้นนี้ได้ แต่ระบบจะใช้รหัสเริ่มต้น `ward1234` ซึ่ง **ห้ามใช้กับข้อมูลผู้ป่วยจริง**

**2. กดลิงก์นี้เพื่อสร้างเครื่องเซิร์ฟเวอร์**

👉 **https://codespaces.new/Kingcondom/Ward**

กด **Create codespace** แล้วรอ 1–2 นาที ระบบจะติดตั้ง Node, ใส่เคสตัวอย่าง 7 เตียง
สตาร์ทเซิร์ฟเวอร์ และ **เปิดหน้าเว็บให้เอง**

ถ้าหน้าเว็บไม่เด้งขึ้น: แถบล่าง → แท็บ **Ports** → กดไอคอนรูปโลก 🌐 ที่พอร์ต **3000**

**3. Login**

- **ชื่อผู้บันทึก** — พิมพ์ชื่อตัวเอง (ชื่อนี้จะไปติดอยู่กับ SOAP ที่บันทึก)
- **รหัสผ่าน** — รหัสที่ตั้งไว้ในข้อ 1

**4. ล้างเคสตัวอย่างก่อนใส่เคสจริง**

ที่หน้า VS Code → เมนู ☰ → **Terminal → New Terminal** → พิมพ์

```bash
npm run clear
```

7 เตียงสมมติจะหายไปหมด เหลือหอผู้ป่วยว่าง ๆ (เซิร์ฟเวอร์ยังรันอยู่ ไม่ต้องปิด แค่ refresh หน้าเว็บ)

**5. นำเคสจริงเข้า**

ในเว็บ → ปุ่ม **💾 สำรอง / นำเข้า** บนหน้าแรก → ช่อง **นำเข้าเคสทั้งชุดจากไฟล์** →
เลือกไฟล์ `.json` ที่เตรียมไว้ → ตรวจรายการเคสที่ขึ้นมา → **นำเข้าทั้งหมด**

ถ้าเตียงนั้นมีผู้ป่วยอยู่แล้ว ให้ติ๊ก **เขียนทับถ้าเตียงนั้นมีผู้ป่วยอยู่แล้ว**

**6. เซฟไว้ที่หน้าจอหลัก iPad**

คัดลอกลิงก์ของ **ตัวเว็บ** (ไม่ใช่หน้า VS Code) — รูปแบบคือ

```
https://<ชื่อ codespace>-3000.app.github.dev
```

เปิดใน **Safari** → ปุ่มแชร์ → **Add to Home Screen** จะได้ไอคอนเปิดตรงเข้าเว็บแบบเต็มจอ

### วันหลังจะกลับเข้าใช้

1. กดไอคอนที่หน้าจอหลัก ถ้าเปิดได้เลย = เสร็จ
2. ถ้าขึ้นว่าเปิดไม่ได้ แปลว่า codespace หลับอยู่ → เปิด https://github.com/codespaces →
   กดชื่อ codespace **เดิม** → รอสักครู่ให้มันตื่น → แล้วกดไอคอนใหม่อีกครั้ง

> ⚠️ **อย่ากดสร้าง codespace ใหม่** ถ้ายังไม่ได้สำรองข้อมูล — ของใหม่จะได้ลิงก์ใหม่และข้อมูลว่างเปล่า

### ให้เพื่อนร่วมทีมเข้าด้วย

แท็บ **Ports** → คลิกขวาที่พอร์ต 3000 → **Port Visibility** → **Public** → ส่งลิงก์ให้เพื่อน

> ⚠️ Public = ใครมีลิงก์ก็เข้าถึงหน้า login ได้ ต้องตั้ง `WARD_PASSCODE` เองก่อนเสมอ
> และถ้าเป็นข้อมูลผู้ป่วยจริง ควรใช้วิธี ข. แทน

---

## ข. มีคอมพิวเตอร์ในวง Wi-Fi เดียวกัน (แนะนำสำหรับข้อมูลผู้ป่วยจริง)

ข้อมูลไม่ออกนอกเครือข่ายโรงพยาบาลเลย

บนคอม (ต้องมี Node.js 22.5 ขึ้นไป):

```bash
git clone https://github.com/Kingcondom/Ward.git
cd Ward
npm run clear                                  # ล้างให้ว่าง (ข้ามได้ถ้าอยากดูเคสตัวอย่างก่อน)
WARD_PASSCODE='รหัสที่ตั้งเอง' npm start
```

หาไอพีของคอม — macOS: `ipconfig getifaddr en0` · Windows: `ipconfig` ดู IPv4 · Linux: `hostname -I`

บน iPad/มือถือเปิด Safari ไปที่ `http://<ไอพี>:3000` เช่น `http://192.168.1.42:3000`
ทุกเครื่องเปิดลิงก์เดียวกันได้พร้อมกัน ข้อมูลซิงก์ real-time

> เปิดไม่ได้ → อนุญาต Node ผ่าน firewall และเช็กว่าอยู่ Wi-Fi วงเดียวกัน (ไม่ใช่ Guest network)

---

## 💾 สำรองข้อมูลทุกครั้งที่บันทึกของสำคัญ

ในเว็บ → ปุ่ม **💾 สำรอง / นำเข้า** บนหน้าแรก → **ดาวน์โหลดข้อมูลทั้งหมด**
จะได้ไฟล์ `ward-backup-YYYY-MM-DD.json` เก็บไว้ใน Files

ข้อมูลอยู่ในไฟล์ `data/ward.db` ของเครื่องที่รันเท่านั้น **ถ้าลบ codespace หรือลบเครื่องนั้น ข้อมูลหายหมด**
ไฟล์สำรองนำกลับเข้าระบบได้ที่แท็บเดียวกัน

## ⚠️ ข้อมูลผู้ป่วยจริง

- ใช้ **วิธี ข.** (รันในเครือข่ายโรงพยาบาล) สำหรับข้อมูลจริง — ขึ้นคลาวด์สาธารณะมีผลทาง PDPA
- กรอกแค่ **ชื่อย่อ** ตามที่ระบบออกแบบไว้ อย่าใส่ชื่อ-สกุลเต็ม
- ตั้ง `WARD_PASSCODE` ให้เดายาก และเปลี่ยนเมื่อมีคนย้ายออกจากทีม
- ไฟล์ข้อมูลผู้ป่วยที่ดาวน์โหลดมา **ห้าม commit ขึ้น GitHub** (`.gitignore` กันชื่อ `patient-data*`,
  `private-*`, `*.phi.*` ไว้ให้แล้ว — ตั้งชื่อไฟล์ตามนี้จะปลอดภัยที่สุด)
